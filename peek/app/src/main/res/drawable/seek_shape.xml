<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- 背景图 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dp" />
            <!--

                android:endColor="#FBFBFB"
                android:centerColor="#9E9D9D"
                android:startColor="#626060"-->
            <solid android:color="@color/background"/>

            <gradient
                android:angle="0"
                android:endColor="@color/background"
                android:centerColor="@color/background"
                android:startColor="@color/background" />


        </shape>
    </item>


    <!-- 第二进度条的背景色 -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="@color/background" />
            </shape>
        </clip>
    </item>


    <!-- <item android:id="@+android:id/background" android:drawable="@drawable/bar_dn" /> -->
    <!-- 可拖动的进度条颜色 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="10dp" />
                <gradient
                    android:angle="0"
                    android:centerColor="#FFB20D"
                    android:endColor="#FFDF0D"
                    android:startColor="#FF7F0D" />
            </shape>
        </clip>
    </item>

</layer-list>